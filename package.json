{
  "name": "looprail-repo",
  "private": true,
  "description": "Repository-level tooling for Looprail.",
  "license": "Apache-2.0",
  "scripts": {
    "commitlint": "commitlint --config commitlint.config.cjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.1.0"
  }
}
