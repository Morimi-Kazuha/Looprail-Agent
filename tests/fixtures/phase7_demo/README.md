# Phase 7 CLI demo fixture

This tiny repository is copied into a temporary workspace by the Phase 7
acceptance tests. `calculator.py` contains a deliberately failing `add`
implementation and `test_calculator.py` is the invariant test that the
deterministic Provider repairs it without editing the test.
